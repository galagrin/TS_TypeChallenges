# 📘 TypeScript Type Challenges: Шпаргалка

Эта памятка помогает систематизировать знания для решения задач на [Type Challenges](https://github.com/type-challenges/type-challenges).
Здесь собраны основные инструменты для манипуляции типами, работающие как "программирование внутри компилятора".

---

## 🛠 Инструментарий

### 1. Дженерики (`<T>`) — Аргументы

Как функция принимает аргументы, так и тип принимает дженерики.

-   **JS:** `function echo(data) { ... }`
-   **TS:** `type Echo<T> = ...`

### 2. `keyof` — Получение ключей

Берет объект и возвращает **объединение (Union)** его ключей. Аналог `Object.keys()`.

```ts
interface User {
    name: string;
    age: number;
}
type Keys = keyof User; // 'name' | 'age'
```

### 3. Mapped Types ([P in K]) — Цикл

Главный способ создания новых объектов. Аналог цикла for (let key in obj).

```ts
// Читается: "Пробегись по всем ключам в K..."
type MyType<K> = {
    [Key in K]: string; // ...и создай поле с типом string
};
```

### 4. Indexed Access (T[K]) — это доступ к свойству obj[key]

Как в JS пишешь user['name'], чтобы получить значение, так и в TS ты пишешь User['name'], чтобы получить тип этого значения.

```ts
interface User {
    id: number;
    name: string;
}

type NameType = User["name"]; // Результат: string
```

Внутри цикла (Mapped Types) это супер-сила:

-   Если мы перебираем ключи [P in keyof T], то T[P] означает "тип значения, который лежит под текущим ключом P".

### 5. Extends — Ограничение или Условие

Имеет два смысла в зависимости от контекста:

А. Ограничение (Constraint): "Я принимаю только..."

```ts
// T обязан быть массивом
type MyFunc<T extends any[]> = ...
```

Б. Условие (Conditional Type): Тернарный оператор

```ts
// Если T строка — верни true, иначе false
type IsString<T> = T extends string ? true : false;
```

### 6. Магия массивов (T[number])

Способ превратить массив значений в объединение этих значений.

```ts
type Colors = ["red", "blue", "green"];
type Union = Colors[number]; // 'red' | 'blue' | 'green'
```

### 7. `infer` — Объявление переменной внутри условия

Ключевое слово `infer` работает **только** внутри `extends`. Оно позволяет "вытащить" тип изнутри другого типа и записать его в переменную.

**Аналогия с JS:**

```js
// Если T соответствует шаблону Promise<...>,
// то объяви переменную R равную содержимому и верни её.
if (T matches Promise<R>) {
  return R;
}
```

Пример:

```ts
type Unpack<T> = T extends Promise<infer R> ? R : T;
// Unpack<Promise<string>> -> string
```

### 8. Рекурсия в типах

Тип может вызывать сам себя. Это нужно, чтобы добраться до дна вложенности (например, Promise<Promise<Promise<string>>>).

Паттерн:

```ts
type Recursive<T> = T extends Condition
    ? Recursive<InnerType> // ⟳ Вызываем сами себя для внутреннего типа
    : T; // ⏹ Базовый случай (дно рекурсии)
```

## 🧠 Алгоритм решения задач

Когда видишь задачу на преобразование типа, задай себе вопросы по шагам:

-   Шаг 1: Что на входе?
    Объект: Используем keyof T для получения ключей.
    Массив: Используем T[number] для получения значений.
-   Шаг 2: Создаем каркас цикла
    Почти любая задача начинается с этой конструкции:

```ts
{
  [P in ...]: ...
}
```

-   Шаг 3: Что перебираем (левая часть)?
    Если вход Объект → [P in keyof T] (ключи: "title", "desc")
    Если вход Массив → [P in T[number]] (значения: "tesla", "model 3")
    Если дан список ключей K → [P in K]
-   Шаг 4: Чему равно значение (правая часть)?
    Нужно сохранить исходный тип? → T[P]
    Нужно, чтобы значение совпадало с ключом? → P
    Нужно зафиксировать тип? → boolean, string и т.д.

## ⚡️ Примеры паттернов

| Задача                                   | Код решения                                       | Логика                                                                                                                    |
| :--------------------------------------- | :------------------------------------------------ | :------------------------------------------------------------------------------------------------------------------------ |
| **Pick**<br>_(Выбрать поля K из T)_      | `{ [P in K]: T[P] }`                              | 1. Пробегись по ключам `K`<br>2. Возьми типы значений из `T`                                                              |
| **Readonly**<br>_(Запретить запись)_     | `{ readonly [P in keyof T]: T[P] }`               | 1. Пробегись по всем ключам `T`<br>2. Добавь `readonly`<br>3. Оставь значения как были                                    |
| **TupleToObject**<br>_(Массив в объект)_ | `{ [P in T[number]]: P }`                         | 1. Пробегись по значениям массива (`T[number]`)<br>2. Сделай значение равным ключу (`P`)                                  |
| **Exclude**<br>_(Убрать U из T)_         | `T extends U ? never : T`                         | Если T совпадает с U — удали (never), иначе оставь T.                                                                     |
| **Awaitedv**<br>_(Распаковать Promise)_  | `T extends PromiseLike<infer R> ? Awaited<R> : T` | 1. Это промис? Если да, вытащи нутро в R.<br>2. Запусти проверку заново для R (рекурсия).<br>3. Если не промис — верни T. |
