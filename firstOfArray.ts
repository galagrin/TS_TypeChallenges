// Реализуйте дженерик First<T>, который принимает массив T и возвращает тип его первого элемента.

// Если массив пуст, тип должен возвращать never.

// Пример использования:

//   type arr1 = ['a', 'b', 'c']
//   type arr2 = [3, 2, 1]

// type head1 = First<arr1>; // ожидается 'a'
// type head2 = First<arr2>; // ожидается 3

//Вариант 1: Проверка на пустой массив (Самый простой)
//Мы используем условный тип (Conditional Type), чтобы проверить, соответствует ли T пустому кортежу [].
type First<T extends any[]> = T extends [] ? never : T[0];

// Логика работы:

// T extends []: Задаем вопрос: "Выглядит ли наш массив T как пустой массив []?".
// ? never: Если Да (массив пуст), возвращаем тип never (означает "невозможное значение").
// : T[0]: Если Нет (в массиве что-то есть), возвращаем тип элемента под индексом 0.

//Вариант 2: Проверка длины массива
// Мы можем проверить свойство length у массива.

type First1<T extends any[]> = T["length"] extends 0 ? never : T[0];

// Логика работы:

// T['length']: Получаем длину массива. Для [1, 2] это будет тип 2. Для [] это будет тип 0.
// extends 0: Спрашиваем: "Равна ли длина нулю?".
// Дальше логика аналогична первому варианту.

// 📚 Полезные концепции
// Indexed Access Types (T[0]): Доступ к типу элемента по индексу.
// Conditional Types (extends ? :): Ветвление логики (аналог if/else).
// Tuple types: Работа с кортежами и их свойствами (например, length).
